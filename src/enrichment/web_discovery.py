"""v2 stub — web-assisted transcript discovery (not wired in v1)."""


def discover_transcript_url(
    ticker: str,
    quarter_label: str,
    *,
    call_date: str | None = None,
) -> str | None:
    return None
