---
id: chec-1338b1c1
type: checkpoint
project: earnings-call-summarizer
parent_id: deci-b76fadf8
title: 'PAUSED mid-Step 1: transcript indexer v2'
node_label: 'PAUSED mid-Step 1: transcript indexer v2'
tags: retrieval-first,index,paused,transcripts
status: active
open_threads: 0
success: 'null'
files: ''
session_id: sess-397220c2
created_at: '2026-09-01T21:18:17.389968+00:00'
updated_at: '2026-09-01T21:18:17.389968+00:00'
---
Paused 2026-09-01 ~17:20 ET at the user's request (leaving office; resume in a couple of hours). Nothing committed.

## What changed in scripts/_desk_transcript_index.py (INDEX_VERSION bumped 1 -> 2; file parses, NOT yet rebuilt)
- Predecessor-based role inference for Q&A-only speakers: first turn after an operator turn -> analyst; after a management/analyst/IR turn -> management (a colleague handing over the answer, e.g. 'Roopal?'). Question ratio is now only the tiebreak. On the v2-partial rebuild this took unknown speakers from 1,385 -> 4 across 844 indexes (e.g. Christi Shaw at LLY, Carrie Strom / Roopal Thakkar at ABBV now management).
- Unlabeled transcripts (no speaker attribution anywhere, e.g. CI_FY2019-Q2, JNJ_FY2020-Q2): one paragraph per turn, speaker 'Unknown Speaker', role unknown, `unlabeled: true` flag in the index, Q&A boundary from unlabeled_qa_start().
- Per-ticker raw layout now consumed: `transcripts_raw/{TICKER}/{PERIOD}.txt` (older pipeline pulls, July 2026) alongside the flat `{TICKER}_{PERIOD}.txt`. Flat wins when both exist. 770 extra transcripts live only in subdirs (20 ops tickers FY2016+, e.g. MSFT/FY2017-Q2.txt — the 'lost' MSFT BUILD transcript exists after all). New helpers raw_candidates() / raw_path(ticker, period, min_bytes).
- Header-block skip ('CTSH Q1 FY2026 Earnings Call Transcript', 'Date:', 'Source:', '====' lines; 128 files in that format).
- Bare-name labels ('Operator', 'Tyler Scott' alone on a line, blank lines either side).
- `Name, Firm:` inline labels and a leading '[Operator instructions]' tag; a second speaker embedded on the operator's line ('Operator: [Operator instructions] Keith Weiss, Morgan Stanley: ...') gets its own turn.
- New _OPERATOR_HANDOFF_RE (anchored: 'Next, we have Carter Gould from UBS.' / 'Vamil Divan, Guggenheim Securities.') used for the Q&A flip and analyst naming; _QA_FLIP_RE gained 'if you have a question ... press star', 'move over to Q&A', 'ready for questions'. Q&A flip now requires at least one non-operator turn before it (opening operator turn always previews Q&A).

## Resume steps (in order)
a. `python scripts/_desk_transcript_index.py` — full rebuild (~1,600 files incl. the 770 subdir ones; ~2-3 min).
b. Re-run the stats probe (C:\Users\BobbyWhittaker\AppData\Local\Temp\index_stats.py, unknown_probe.py, raw_probe.py — recreate if Temp was cleared) and check the 7 no-QA-flip files: MSFT_FY2025-Q3, ABBV_FY2023-Q2, REGN_FY2018-Q1, CTSH_FY2026-Q1 (+ ABBV_FY2026-Q2, JNJ_FY2022-Q3, LLY_FY2017-Q1, PFE_FY2025-Q1) and the unlabeled CI_FY2019-Q2 / JNJ_FY2020-Q2. Expect unknown roles ~4, no-management ~2-3.
c. Point scripts/_desk_transcript_backfill.py::raw_present at _desk_transcript_index.raw_path(ticker, period, MIN_RAW_BYTES) so the manifest stops requesting transcripts already present in subdirs (P2 missing count of 596 will drop sharply).
d. Continue with Step 2: scripts/_desk_retrieval.py per retrieval_first_scoring_c4e81f2a.plan.md (match block, alias derivation, generic inference, Tier 0-3 ladder, ranking, --alias-audit), then catalog-match, scorer rewire, seed-match, workshop HTML, tests.

## Remaining estimate
~4 h wall-clock at this session's tool latency (index fixes 20 min, retrieval 45, catalog tuning 40, scorer 60, seed-match 15, workshop 25, tests + --plan dry run 30). Live budgeted scoring run not included; run in --plan mode first.
