---
id: chec-03e2f056
type: checkpoint
project: earnings-call-summarizer
parent_id: plan-c35843a9
title: 'Committing the monitor automation: 187 MB transcript index caught before it
  entered history'
node_label: 'Committing the monitor automation: 187 MB transcri'
tags: git,commit,monitor,automation,gitignore
status: active
open_threads: 0
success: 'null'
files: ''
session_id: sess-64604bc2
created_at: '2026-09-02T19:21:54.442770+00:00'
updated_at: '2026-09-02T19:21:54.442770+00:00'
---
Committing this workstream to cursor/call-ticker-reaction. Recorded before running git, per protocol.

WHAT SHIPS (monitor automation, Phases 1-3b + scheduler):
- eligibility.py: monitored universe = overlays UNION in-code registry, minus explicit excludes. Took the watched set from 5 to 45 companies.
- service.py: _absorb_reschedule for reissued provider IDs, and the Phase 3b state guard so a COMPLETE quarter's recorded call time is never rewritten by a late Quartr correction.
- quartr_oauth.py + quartr_mcp.py (new): headless Quartr access via RFC 7591 registration, PKCE and a refresh token.
- calendar_publish.py: dedupe_by_period, report_at clamping, per-manifest error isolation, and change detection (call_at / previous_call_at / rescheduled) that makes a moved date visible.
- host_automation.py: quartr login|status|tools, calendar --source mcp, watchlist sync, --verify-within-days.
- scripts/run_quartr_sweep.ps1 (new): Task Scheduler wrapper, 6am daily.
- Tests: 854 passing.

WHAT IS DELIBERATELY EXCLUDED:
- .cursor/mcp.json -- user's explicit instruction; it carries the machine-specific angelo-venv path.
- .cursor/mcp.json.bak-* -- local backup of the same.
- Structured Narrative/transcripts_index/ -- 187 MB across 1,578 JSON files, NOT previously gitignored. A blanket 'commit everything' would have pushed this into permanent history. It is regenerable by scripts/_desk_transcript_index.py and is licensed transcript content, so it falls squarely under the repo's existing policy ('Regenerable transcript / filing corpus - Angelo holds briefs, not the bytes'; 'not committed (licensing)'). Added to .gitignore alongside data/transcripts_backfill/ (6.5 MB).

That gitignore gap is the reusable lesson: the repo already excluded transcripts_raw/ and the per-ticker raw folders, but a NEW transcript directory introduced by later tooling inherited no protection. Any script that writes a new corpus directory needs a matching ignore rule in the same change, or the next 'git add -A' commits it.

The desk/claims retrieval work in the tree is a separate workstream from an earlier session and is committed separately rather than bundled.
